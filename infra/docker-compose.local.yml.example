services:
  postgres:
    image: postgres:16
    container_name: qr_tickets_postgres
    restart: unless-stopped
    env_file:
      - .env.local
    ports:
      - "5434:5432"
    volumes:
      - qr_tickets_pgdata:/var/lib/postgresql/data

volumes:
  qr_tickets_pgdata:
